'''
'''


'''
面试题：
1、langchain 的6大组件是什么？
1. 模型（Models）相当于Ai应用的大脑，包含 LLMs（纯文本模型）和 Chat Models（对话模型）
作用： 负责接收指令并生成回复。它是所有逻辑的执行者
2. 提示词（Prompts）相当于给大脑的说明书，包含 PromptTemplate 和 ChatPromptTemplate
作用： 把用户的零散输入，包装成 AI 能听懂的、带有角色设定和格式要求的完整指令
3.数据连接/检索（Data Connection / Retrieval）相当于Ai的外部图书馆，包含文档加载器（Loaders）
文本分割器（Splitters）、向量数据库（Vector Stores）和检索器（Retrievers）
作用:AI 的大脑知识是有限的。通过这个组件，可以让 AI 读取 PDF、数据库或网页，实现 RAG（检索增强生成）
4.链（Chains）相当于各组件之间的粘合剂，在新版本中主要指 （管道符 |），在旧版本中指 LLMChain
作用： 把提示词、模型、解析器等零件串联成一个自动化的流水线
5.记忆（Memory）相当于Ai的笔记本，包含ChatMessageHistory、ConversationBufferMemory等
作用:Memory 组件负责存储之前的对话上下文，让 AI 能在第 10 轮对话时还记得你第 1 轮说了什么
6.代理（Agents）自定义Ai的手脚，包含AgentExecutor、Tools（如 Google 搜索、Python 解释器）
作用：它让 AI 能够自主决定下一步该做什么，主动调用“搜索工具”去查，最后再回答


2、什么是提示词，它在与大模型交互中的作用是什么？
提示词的组件：1.角色（Role）：告诉 AI “你是谁”，2.上下文（Context）：给它背景资料
3.任务（Task）：明确要干什么，4.约束（Constraints）：规定不能干什么
总结:大模型（LLM）就像是一个拥有海量知识但极其“听话”的实习生。提示词就是你写给他的那张任务清单


3、在langchain中写提示词，form_template与form_messages有什么区别，分别在什么场景下使用
from_template:它把所有的指令、背景、变量全塞进一个字符串里，最终发给 AI 的就是一个大文本块，
生成的对象： PromptTemplate
from_messages:它把提示词拆成了不同的角色（Role）,最终发给 AI 的是一个列表
包含 SystemMessage（系统设定）、HumanMessage（用户提问）等
生成的对象： ChatPromptTemplate
（使用场景）
什么时候用 from_template:老式的、只会根据上文“接话”的模型（LLMs）
什么时候用 from_messages:绝大多数场景。 现在的模型基本都是 Chat 模式


4、你们项目中格式解析器都用什么？如何保证输出的一定是JSON
JsonOutputParser + Pydantic
JsonOutputParser：它是 LangChain 官方推荐的解析器。
它不仅负责把字符串转成字典，还能自动生成一段极其严谨的“格式说明”喂给 AI
Pydantic (BaseModel)：这是 Python 里定义数据结构的标杆。
用它写一个类，规定好有哪些字段、是字符串还是数字

5.如何保证输出的一定是JSON
1.使用 Pydantic 定义,明确告诉解析器，想要什么样的 JSON
2.注入 format_instructions
get_format_instructions()。它会生成一段复杂的指令，告诉 AI：“你必须输出一个符合特定 Schema 的 JSON
3.使用 OutputFixingParser
如果 AI少写了个引号或者括号，导致JSON解析失败，套一层OutputFixingParser
它发现解析报错后，会自动把错误原因和原始文本再发给 AI一次，让Ai按照正确格式修正

'''

'''
面试题：
一、在你们项目中，你们常用的runnable模块有哪些？
1.Runnable 顶层的抽象基类，定义了一个组件“能跑起来”的所有标准动作
2.RunnableSerializable （可运行）Runnable 它可以实现invoke、batch、stream
（可序列化）Serializable这个组件可以被完整地保存下来（变成 JSON 或磁盘文件），以后还能原封不动地加载回来
3.RunnableParallel可运行可并行，同时运行多个组件，并将结果合并成一个字典
4.RunnablePassthrough可运行直通，把接收到的输入原样传给下一步，不做任何修改
5.RunnableConfig运行时的“配置手册”，一些控制参数(如：这次调用用哪个 API Key？允许重试几次？)这些参数都塞在 RunnableConfig

一（2）、在你们项目中，你们常用的runnable可运行组件有哪些？
大模型、提示词模板、输出解析器、管道符、检索器、cohere、文档加载器分割器、嵌入模型、向量库、runnableLambda、langgraph也算

二、你们项目中使用什么来监控大模型的运行状态？
1. LangSmith，LangChain的官方平台
监控内容:可视化追踪，可以看到数据如何流过提示词--LLM--解析器
延迟监控：记录链条中每一跳（Hop）耗时多久
成本统计：自动计算每次调用的 Token 消耗和估算费用

2.Callback(自定义实时监控)
StdOutCallbackHandler:直接在终端看 AI 的“思考过程”
FileCallbackHandler:运行状态存入本地 .log 文件

三、什么是TTFT？
中文含义：首字时间（或首个 Token 生成时间）,
计算方式：从用户发出请求（点击发送）那一刻起，到模型吐出第一个字符传回客户端所经历的时间。
感官体验：如果 TTFT 很长，用户会觉得 AI 在“发呆”或网络卡顿；如果 TTFT 短，即使后面生成速度慢，用户也会觉得 AI 反应很快。

四、如何降低 TFTT 对 agent 的影响？
1.精简上下文,大模型处理 Prompt 的过程是非线性的，Prompt 越长，预处理阶段的耗时越久。
做法：清理不必要的历史对话（只保留最近 5-10 轮）；精简 System Prompt，避免冗长的、重复的指令。
2.模型分级,不是所有任务都需要 GPT-4以上的模型
做法：使用一个极快的轻量模型（如 GPT-4o-mini）作为“路由”或“预处理器”
3.感知优化，开启流式传输
做法：在前端实现“打字机”效果。即使模型还没完全写完那个复杂的 JSON，只要第一个括号蹦出来，用户就知道系统在工作。

'''

'''
面试题:
你们大模型中的记忆是怎么做的？ 
请回答记忆的多种策略，以及使用的场景？
在大型语言模型（LLM）的开发框架中，记忆（Memory） 的本质是将历史对话作为上下文（Context）重新喂给模型
1.全量记忆 (Conversation Buffer Memory)
将所有的历史对话（Human 和 AI 的原话）原封不动地全部存下来
工作方式：每次新对话时，把之前的 (Human 和 AI 的对话) 全部推给模型
缺点：对话一旦变长，Token 消耗会呈爆炸式增长，最终导致费用高昂

2. 滑动窗口记忆 (Conversation Buffer Window Memory)
只记住最近的 第k 轮对话，丢弃更早的记忆
工作方式：设置 k=5，当第 6 轮对话开始时，第 1 轮对话就会被从“窗户”里丢出去
缺点：AI 会表现得非常“健忘”，一旦聊深了，它就不记得最初设定的目标或你的名字

3.令牌限制记忆 (Conversation Token Buffer Memory)
按“Token 长度”删记忆
工作方式：设置限制为 2000 个 Token。只要历史记录超过这个数，就从最旧的消息开始删除，直到剩下的长度达标
优点：比窗口记忆更科学，因为有些对话虽然轮数多但很短

4.摘要缓冲区记忆 (Conversation Summary Buffer Memory)
近期存细节，远期存摘要
工作方式：最近的对话保留原文，当对话超过一定长度时，调用另一个 LLM 将旧对话压缩成一段简短的摘要
最终发送给模型的是：[过去对话的摘要] + [最近几轮的原文]
优点：在节省空间和保留历史意义之间达到了完美的平衡

5.实体记忆 (Conversation Entity Memory)
“对象化记录”，是按“提到的东西”存记忆
作方式：AI 会从对话中识别出特定的实体，并为这些实体建立“档案卡片”/实体存储(entity_store)
优点：适合处理涉及多个特定对象、参数众多的复杂任务

'''


'''
面试题:
一、你们项目中的向量数据库是怎么做的？
1. 架构选型:
项目中搭建了一套RAG（检索增强生成）架构。核心是把文字信息转化成‘数字坐标’（向量）存进数据库。
工具选型：本地开发用了 FAISS，云端部署用了 Pinecone、TCVectorDB（腾讯云）、Weaviate（Docker环境）
模型选型：OpenAI的text-embedding-3-small，以及BAAI的BGE-M3，
2. 数据处理：
切分逻辑：把长文章切成一小段（分块），每段控制在几行字的长度，方便精准定位。
留衔接位：在切分时，每一段的末尾和下一段的开头会留一段重叠区，
防止一句话被劈成两半导致意思断掉。这样能保证 AI 检索时上下文是连贯的。
3. 检索优化
独立隔间：利用命名空间把不同类的数据分开存。
过滤：设置了一个相似度门槛分数。如果找出来的记录与问题‘意思差得太远’，系统会自动把它过滤掉

二、你们的向量数据库选择的匹配方式是什么
1. 最常用的是 余弦相似度
核心逻辑：它不看向量的绝对长度，只看两个向量之间的夹角
理解：只要方向一致，哪怕一个字数多，一个字数少，它们也被认为是极其相似的。
适用场景：文本检索、问答系统。它能很好地处理长短不一的句子，只要意思对就行。
数值区间：通常在 -1 到 1 之间（在 Embedding 中常表现为 0 到 1），越接近 1 越相关
2.欧几里得距离
核心逻辑：测量两个点在空间里的绝对直线长度
理解：两个点离得越近，就越像。它不仅受方向影响，还受向量长度的影响。
适用场景：人脸识别、图片搜索。在这些场景中，光照、尺寸等“绝对数值”非常重要。
数值区间：0 到正无穷，越接近 0 越相关（0 代表完全重合）
3.内积
核心逻辑：向量对应位置相乘再相加
理解：它既考虑了方向，也考虑了向量的权重。如果某个特征的数值非常大，它会极大拉高相似度
适用场景：推荐系统（比如点击率预测）。如果一个商品不仅符合用户口味，而且本身热度很高（向量模长长），内积会优先把它排在前面
数值区间：数值越大，关联性越强

三、你们项目中使用的是那种向量模型？
OpenAI的text-embedding-3-small，以及BAAI的BGE-M3
OpenAI的text-embedding-3-small英文表现力不错，向量维度为1536，按TOKEN计费
BAAI的BGE-M3专门针对中文优化，向量维度为1024，完全免费，它支持多功能检索，
不仅能给出一个坐标，还能像传统搜索一样看关键词和关键字符。

四、什么是向量？
向量就是把文字信息“翻译”成的一串数字坐标，在电脑眼里，它不再是汉字，而是一个在多维空间里的点
它的作用：让电脑能通过“数学计算”来理解“语义”
它的特点：意思越接近的词，在空间里的距离就越近

五、如果需要切换向量模型我们需要怎么做？
1. 彻底清空或重建数据库。删除旧的索引，然后创建一个维度匹配的新索引
2. 重新跑一遍入库流程。原始文本需要用新模型重新读取并转化一遍
3.更新代码中的适配器接口。更换模型初始化代码，并更新在项目中写的适配器类

六、什么是向量模型？
向量模型是一个将“人类语言”映射到“数学空间”的数学函数
本质：把“语意”变成“坐标”，向量模型的作用，是给每一个词语在空间里找一个精确的坐标点
维度:以BGE-M3为例，它生成的是 1024 维的向量。这意味着它用1024个数字来描述一个词。
总结:把所有文档通过模型转成坐标存进数据库，模型把输入的提问也转成一个坐标，
数据库会在空间里寻找离你提问坐标最近的文档坐标。

'''

'''
面试题
一、你们项目中既然用到了文档加载器，你了解那些加载器？
加载阶段 (Loaders)
TextLoader: 最基础的加载器，直接读取 .txt 等纯文本
高频内置加载器: LangChain 提供了针对 PDF、Markdown、JSON、CSV等各种格式的专用工具
自定义加载器: 通过继承 BaseLoader 并实现 lazy_load方法，可以自己写逻辑去解析数据
Blob 与 BlobParser:Blob代表原始二进制数据，这种解耦方式能更灵活地处理不同来源（如流数据、云存储）的内容，是替代传统加载器的高级方案。
分割阶段
二、你们在做知识库的时候使用到了分片是怎么做的？
分割阶段(Splitters)
字符分割器: 按固定长度或单个字符切割。
递归字符文本分割器:它会尝试按段落、句子、单词的优先级递归切割，尽量保持语义连贯性，不把一句话切断。
语义文档分割器:用 Embedding（嵌入），计算句子间的相关性，只有当话题发生变化时才切分
自定义分割器:根据特定业务逻辑（比如按特定的正则表达式）来定制切分规则
三、你讲到知识库的分词，这是怎么做的？
1.基于 Token 的分片（计算长度），分割器调用大模型的专用分词算法，确保切出来的每一片内容都不会超过大模型的 Context Window（上下文窗口）限制
2.向量化前的预处理（Embedding），把文字存入向量库之前，Embedding模型会进行内部编码:
规范化：去除多余空格、统一大小写。切词：根据模型训练好的词表，将文本转化为ID 序列。计算向量：将这些ID序列输入神经网络，产出代表语义的向量坐标

'''

'''
一、除了RRF算法你们还了解其他的算法吗？常数为什么取60？你们做过调优吗？
1.其他算法：Weighted Scoring (加权评分)
核心逻辑：加权就是偏好设置，搜索方式 A（性能）： 专门搜 CPU、显卡、内存配置，搜索方式 B（颜值）： 专门看外观设计、屏幕厚度、颜色
如果你是一个性能控，你可能会给“性能”分数的权重设为 0.8，给“颜值”设为 0.2。总分 = (性能分 × 0.8) + (颜值分 × 0.2)
2.为什么常数取 60？
k=60 是基于论文经验得出的。它的作用是平滑权重：如果k很小，第 1 名和第 10 名的差距会非常巨大
3.调优经验：
在处理垂直/特定领域等专业领域时，如果关键词检索（BM25）非常准，我们会尝试调小 k（如 20-40），让 Top 1 的权重更突出

二、你说了解到 查询转换阶段 6种策略，能分别说说吗？
这 6 种策略的核心逻辑是：用户提问不一定适合直接检索，需要经过“加工”再搜
1.Rewrite (重写)：纠正错别字、补全上下文
2.Expansion (扩张)：加入同义词（如将“显卡”扩张为“芯片”）
3.Decomposition (拆解)：将复杂问题拆成多个子问题
4.Step-back (回退)：从具体问题抽象出基础原理
5.HyDE (虚构文档)：先生成假答案，用假答案搜真文档
6.Sub-query (子查询)：针对不同知识库并行发起针对性查询

三、什么是多查询重写策略？
利用大模型将一个 用户提问 改写成 3-5 个意思相近但表达不同的 提问
价值：克服向量搜索的“语义盲区”。不同描述方式在向量空间的位置不同，多查询能更全面地覆盖相关文档

四、什么是多结果融合策略？
将来自不同检索器（如：向量数据库、Elasticsearch、甚至外部 API）返回的多份排名列表，通过 RRF 或加权法合并成一份最终列表
它是“混合检索”的最后一步，解决了“兼听则明”的问题

五、什么是Stepback回退回答策略？
如果直接搜细节搜不到，先让 LLM 问一个更宏观的问题
用户问“等会点什么盖饭好吃？”，系统先回退搜索“本地的美食推荐”。通过基础原理辅助回答具体细节
在 RAG 中，可以将用户提问先写成一个小摘要，用这个“伪文档”去搜数据库里的“真文档”。因为向量模型在处理长度相近的文本时，相似度计算更准确

六、什么是doc-doc 对称检索？
传统的检索是“短句搜长文”（非对称），Doc-Doc 是用长文搜长文
在 RAG 中，可以将用户提问先写成一个小摘要，用这个“伪文档”去搜数据库里的“真文档”。因为向量模型在处理长度相近的文本时，相似度计算更准确

七、什么是Hyde混合检索？
LLM 根据问题生成一篇“假”的百科式回答（虚构文档）
用这个“假答案”的向量去库里搜真正的参考资料

八、什么是混合检索器
一个集成工具，通常包含：语义检索，它主要负责理解语义的意思，关键词检索 (Sparse/BM25)：死磕“术语”

'''

'''
面试题
一、那你了解路由的优化策略吗？
所谓的路由优化，其实就是给RAG系统装上一个智能分拣中枢，通过精准的意图识别，把 Query（查询）分流到最合适的处理链条上。这样做可以提高回复的准确度
1.语义路由
处理高频、固定意图的问题时，更倾向于用语义路由。
不用去调用大模型，而是通过向量相似度对比，把用户的提问直接撞到预设好的意图集上。零Token消耗、毫秒级反馈，是我在优化系统响应速度时的首选
2.逻辑路由
面对跨领域的问题时，利用LLM的Function Calling（函数调用） 做逻辑路由。比如判断这个问题该去查我本地的 Weaviate 向量库，还是该动用 DuckDuckGo 去联网
3.动态模板路由
针对Prompt的优化，根据路由结果自动切换模板。
比如技术纠错类问题用严谨的‘Debug模板’，而产品咨询类则切换到‘引导式模板’。这解决了单一Prompt面对复杂需求时‘注意力涣散’的痛点

二、那你了解问题构建的优化策略吗？
用户的原始提问往往是模糊、口语化的，而底层数据库（如 Weaviate）存储的是高度结构化或专业化的文本，优化的目的就是把一个烂问题重构成一个专业查询
1.查询重写
Multi-Query（多查询转换）利用 LLM 把用户的一个问题衍生出三五个不同维度的同义句。
这样做是为了对冲向量检索的不稳定性，从多个角度去‘撞’知识库，最后用 RRF（倒数排名融合） 把结果合在一起。这比单次搜索的召回率要高得多
2.结构化构建
Self-Querying（自查询）比如用户问2024年关于LLMOps的文档。普通的向量检索可能只搜语义，
利用自查询策略先让 LLM 把问题拆解：一段是语义（LLMOps），一段是元数据过滤器（Year=2024）。这样在检索时就能先通过字段过滤再做向量比对
3.多向量映射
Parent-Document（父子文档）在构建索引时，给一个长段落（父文档）生成很多个精简的小摘要（子文档）或者不同角度的问题。
检索时匹配的是小向量，但返回给模型的是背后的完整大块内容。
4.HyDE
HyDE（假设性文档嵌入）不直接拿问题去搜，而是让 LLM 先针对问题生成一个‘假答案’，再拿这个‘假答案’去搜。
这种‘以答案搜答案’的方法，在某些特定垂直领域，比‘问题搜答案’要准确得多

三、那你了解索引构建的优化策略吗？
传统的暴力切分（Fixed-size Chunking）经常会导致语义断裂，索引优化是为了解决检索噪声与语义完整性的矛盾
1.父子文档架构 在构建索引时，将文档切分为极小的子块用于向量检索，但同时保留它们所属的大块用于上下文生成
子块越小，语义越聚焦，搜索越准；父块越大，给LLM的背景信息越全。这种架构彻底解决了 检索到了片段却丢失了语境 的问题
2.多向量索引 针对同一个文档块生成多个向量，比如：摘要向量、假设性问题向量、以及原始文本向量。
这样，当用户提问时，无论他是问得很直接还是问得很概括，都能从不同维度‘撞’上这个文档
3.递归特征索引 在构建索引时，先对原始块进行聚类并生成摘要，再对摘要进行摘要，形成一套树状结构。
检索时，根据问题的抽象程度，决定是在‘叶子节点’找细节，还是在‘根节点’找大意。这让 RAG 具备了处理长文本总结的能力

四、那你了解检索器的优化策略吗？
本质上是把‘全量搜索’转换成一个‘漏斗模型’。因为向量检索虽然快，但它基于余弦相似度，有时会带回‘语义相关但事实无关’的噪音。
优化思路通常是：先保证召回率，再提升精准度
1.混合检索  单纯的向量检索在处理专有名词时容易翻车。混合检索：把 Weaviate 的向量搜索和传统的 BM25 关键词搜索按权重融合。
这样既能理解用户的意图，又不会漏掉那些生僻但关键的术语。
2.重排机制  先用检索器捞回前 20 条候选文档（粗排），然后引入 Cohere ReRank 这种交叉编码器进行精排
向量模型只是在‘大海捞针’，而重排模型是在‘显微镜下看针’。它能识别出文档里细微的逻辑关系，把真正能回答问题的文档排到 Top 1
3.多样性与冗余消除（MMR 策略）
MMR（最大边界相关性） 算法它在筛选文档时会兼顾‘相关性’和‘多样性’。简单说，就是如果第二名和第一名长得太像，它会跳过去选第三名。
这样能保证喂给模型的信息量是最大的

'''

'''
面试题
一、什么是function_call？有什么方式创建函数工具？
大模型本身只能聊天，但通过函数调用，它可以去查天气、查数据库。就像是给大模型装上了一只手
创建方式：1.LangChain 的 @tool 装饰器、2.Pydantic 定义一个类（Class）来描述函数需要什么参数
3.手写 JSON Schema 用一段字典告诉模型函数名和参数描述

二、function_call里面的函数调用时AI去调用还是人类去调用的？
当AI发现自己回答不了时会吐出一条指令，Python 程序（人类写的逻辑）接到指令，真正去跑代码，程序在把结果喂回给 AI

3、不支持Function_call的大模型如何进行函数调用？
1.如果遇到模型性能太弱，可以用 Prompt（提示词）来硬刚，在提示词里强制格式要求：'你必须按照规定的格式输出'
2.正则表达式：代码拿到模型回复后，用正则把这些关键字段抠出来，手动去对齐

4、如何使用function_call实现JSON输出？
定义一个 Pydantic类作为“工具”，让模型去填充这个类的属性
调用 .with_structured_output(类名)。模型为了能“调通”这个工具，必须强迫自己吐出符合 JSON 结构的参数

5、Langchain 实现 Function_call总共分几步？
1.定义工具：用 @tool 写好函数
2.绑定工具：使用 llm.bind_tools([工具名])
3.模型推理：ai_msg = llm.invoke("问题")，获取 tool_calls
4.执行并回答：根据 tool_calls 运行函数，把结果再传给模型生成人话

6、什么是函数调用，有什么方式创建函数工具
大模型（LLM）本身是一个黑盒，它无法直接联网或操作文件。函数调用就是给模型开一个窗口
模型根据用户提问，选出一个它认为合适的工具，并写出一份“调用申请单”（包含函数名和需要的参数）。
 Python 代码接收到这个申请单，去替它跑程序，再把结果告诉它。


7、大模型在函数调用的时候，出错了怎么办？
1.异常捕获：用 try...except 抓住错误，把报错信息喂回给 AI，跟它说：“你刚才参数给错了，重试一次。”
2.重试机制：LangChain 有专门的 Retry 逻辑
3.参数验证：在执行前，先用 Pydantic 检查参数合法性

8、什么是React智能体？
它是大模型的“思考模版”。模型会不断循环：思考（Thought） -> 行动（Action） -> 观察（Observation）。
思考：思考如何解决问题，行动：尝试用方案A去解决，观察：如果方案A解决不了就用方案B，根据观察到的结果，重新进行思考

9、ReACT的主要缺陷？
1.步数太多（死循环）：模型可能在原地打转，或者推导了很多步还没出结果，费钱又费时
2.容错率低：中间一步观察错了，后面的推理全盘皆错
3.Token 消耗大：每次思考都要带上之前所有的记录

10、什么是多模态，有了解过吗？
就是模型不仅有“耳朵”听文字，还有“眼睛”看图、“嘴巴”说话
输入一张图片，模型能识别图里的文字。
输出：模型能生成图像、音频或视频

'''

'''

第四次课-知识点回顾
一、如何在链应用中动态更改大模型温度参数
1.使用 .configurable_fields，先给模型标记一个“可配置字段”，然后在调用时通过 config 参数动态传入值
需要在同一条链里，通过外部参数频繁改变温度
2.使用 .bind()生成一个新实例，在链的逻辑结构确定后，针对特定步骤固定一个温度
3.LangGraph 场景可以在 节点（Node）内实例化。只有在代码运行到那一刻，根据计算结果才能决定温度

二、如何在链应用中动态选择不同的大模型组件
1.使用 .configurable_alternatives，可以预先定义几个备选模型，然后在调用链时决定用哪一个
2.RunnableLambda (Routing)，使用“路由器”逻辑。程序自动根据用户的输入来选模型

'''
'''
面试题：
一、请总结langchain 跟langgraph 的区别？
langchain是一条一维直线，它只能从左到右，没有办法循环和转弯，langgraph是一张二维平面图，它的边相当于langchain的链，但是可以转弯去任意节点
LangGraph还有个全局账本（State）每个节点留下的数据都会保存在账本里

二、通过langchain实现一个聊天机器人总共分几步？
LCEL模式 
1.选模型 (LLM / Chat Model)，然后把模型对象实例化
2.给模型赋予身份，设计提示词模板 (Prompt Template)；然后定义 SystemMessage（系统消息）和 HumanMessage（用户输入）
3.选定解析器 (Output Parser)，大模型吐出来的是元数据，需要拆包并在链的尾部挂一个解析器
4.链式组装，使用管道符将模型、提示词和解析器串联起来，形成一个自动化的闭环
5.外挂记忆:由于链本身无状态，需使用 RunnableWithMessageHistory，在每次调用时将历史记录注入
6.执行 调用 .invoke()（一次性返回）或 .stream()（打字机模式）
Graph 模式
1.定义状态 (State)，这是机器人的“全量记忆”。不仅存对话，还存 AI 的思考过程、工具调用结果以及用户画像
2.设计节点 (Nodes) 负责调用 LLM 生成回答、负责去数据库、外网或 API 抓取真实数据、负责人为干预（HIL）或逻辑自检
3.边的设计 (Edges) 根据 AI 的输出决定：是直接去下个节点，还是先去调用工具
4.存档点 (Checkpoints) 使用 Checkpointer（如 RedisSaver）。它记录了“时间轴”上的每一个状态，支持断点续传和“时间旅行”回滚
5.人机回环(HIL) 在关键节点设置中断，执行时流程会强制挂起，等待人类在后台审核通过后才继续
6.全链路监控 可以通过 LangSmith，对每一个节点（Node）的耗时、成本和准确率进行监控

三、什么是检查点？有什么作用
图在运行过程中，每一个节点（Node）完成后，对当前 State（状态） 进行的持久化；类似于单机游戏的存档功能

四、langgraph 3要素是什么？
1.State (状态)
图的全局上下文（Schema）。它定义了在整个工作流中传递的数据结构；相当于是一家公司的共享文件，每个部门（节点）干完活都把结果都放这个这里
2.Nodes (节点) 执行逻辑的函数单元。每个节点接收当前的 State 作为输入，执行处理后返回修改后的 State；
相当于是一家公司的一个部门或者某个员工。原子化特性，只负责具体的事务
3.Edges (边) 定义节点之间跳转逻辑的路由。它决定了数据从一个节点处理完后，下一步该去哪；相当于公司的“审批流程”
普通边：硬连接。A 干完一定去 B；条件边：根据State状态的最新情况判断去哪

五、什么是 HIL？
人机协作工作流:在程序运行过程中，AI 不再是一口气跑到黑，而是在特定的节点停下来，等你点个确认或者修改，它才继续往下走
为了解决大模型幻觉，HIL解决了以下三个痛点:1.关键决策审批 2.纠偏与引导 3.时间旅行/回滚

六、如何调试图程序？
核心的调试目标是：看清“状态”（State）是如何在不同“节点”（Node）之间传递和修改的
1.流式状态观察:使用 app.stream 在循环中打印出当前产生的所有消息，观察程序运行的每一个瞬间
2.图拓扑校验:LangGraph 内置了生成图片的功能，可以把逻辑图打出来进行确认
提取蓝图app.get_graph().像素化渲染draw_ascii()
3.执行上下文审查: 当流程进入 agent_node 时，查看输入的 state 里的 messages 列表，确认之前的历史对话是否完整
看 AI 返回的 response 里，那个 .tool_calls 里的参数（Args）
4.全链路监控 利用 LangSmith 平台，记录 AI 思考的每一个步骤、每一个API调用
总结:
逻辑不通用方案 2 看 图（Graph），路连对了没
数据丢了用方案 1 看 流（Stream），哪一步没更新
AI 乱说用方案 3 或 4 看 指令（Tool_calls），是不是给 AI 的规章制度（System Message）没传进去

七、在集群运行过程中如何保证能够在多机器上运行？
1.状态持久化:在单机运行时，状态（State）存在内存里，机器一关就没了。在多机运行中，可以使用 Redis 或 Postgres 作为公共存储
2.环境镜像化：把代码、依赖库、环境变量全部打包成一个镜像 (Image)，然后通过通过 Docker 部署
3.负载均衡与网关:当大量用户涌入时，任务不能全堆在第一台机器上，可以使用 Nginx 或 Kubernetes Ingress。
它们会监测各台机器的压力，把任务平均分配。


'''
